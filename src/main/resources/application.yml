spring:
      application:
                 name: examsimulator
# Oracle Settings
      datasource:
                 url: jdbc:oracle:thin:@localhost:1521/orclpdb1.fios-router.home
                 username: examsimulator
                 password: examsimulator
                 driver-class-name: oracle.jdbc.OracleDriver
      jpa:
         hibernate:
                  ddl-auto: update  
         database-platform: org.hibernate.dialect.Oracle12cDialect
         show-sql: true
         properties:
                 hibernate:
                          format_sql:true
      security:
              user:
                  name: admin
                  password: password                   
                                           
server:
      port: 8085
originurl: http://ebf98d9f9393.ngrok.io           
